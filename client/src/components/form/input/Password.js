import React from 'react';

function Input() {
  return (
    <div>
        <label htmlFor="gen-password-input">Password Input label</label>
        <input type="password" id="gen-password-input" placeholder="Type password"/>
    </div>
  );
}

export default Input;
